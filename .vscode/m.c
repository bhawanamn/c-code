// to find area of triangle

#include<stdio.h>
int main()
{
    float length,breadth;
    printf("Enter length");
    scanf("%f",&length);
    printf("Enter breadth");
    scanf("%f",&breadth);
    printf("area of triangle= %f", 0.5*length*breadth);
    return 0;
}